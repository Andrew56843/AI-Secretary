# callsec Codex Rules

These notes are for Codex sessions working on this project.

- Start with the root `AGENTS.md`; it is the canonical rule file.
- Use `deployment.md` before touching Coolify, Docker, PM2, Asterisk, or `server.js`.
- Use `server-cleanup.md` before deleting anything on the server.

